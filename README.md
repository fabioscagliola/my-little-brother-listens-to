# My little brother listens to

Add your ListenBrainz API URL to the `.env` file and run the following commands:

```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python my_little_brother_listens_to.py
```

